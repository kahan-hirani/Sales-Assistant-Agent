# Server
PORT=3000
NODE_ENV=development

# Groq LLM
GROQ_API_KEY=your_groq_api_key_here

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/sales_agent_db

# Memory backend (currently only 'sequelize' supported — extend in src/memory/index.js)
MEMORY_BACKEND=sequelize
